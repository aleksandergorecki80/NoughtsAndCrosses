import React from 'react';

const ControlButtons = () => {
    return (
        <div>
            <button>Start/Reset</button>
            <button>Save</button>
        </div>
    )
}

export default ControlButtons;